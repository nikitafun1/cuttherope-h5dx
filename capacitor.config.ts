import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
    appId: "com.drunsoft.cuttheropeburmalda",
    appName: "Cut the Rope Burmalda",
    webDir: "dist",
    server: {
        androidScheme: "https"
    }
};

export default config;
